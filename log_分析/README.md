# Claude Code Log 可视化工具

一套用于可视化 Claude Code JSONL 日志文件的完整工具集，支持 Web 界面和命令行两种使用方式。

## 功能特性

- 🌐 **Web 界面** - 基于 Flask 的交互式 Web 服务器
- 📊 **多种日志支持** - History、Session、Subagent 三种日志类型
- 🔍 **智能关联** - 自动发现并显示相关的日志文件
- 📈 **统计图表** - 活动时间线、命令使用统计、工具调用分析
- 💾 **HTML 导出** - 生成可独立使用的 HTML 报告
- 🎨 **美观界面** - 现代化的渐变色设计风格

## 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        Claude Code Log Visualizer System                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                        Web 界面层 (Flask Server)                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │   │
│  │  │ 文件上传    │  │ 快速访问    │  │ 关联文件    │                  │   │
│  │  │ Drag & Drop │  │ Quick Access │  │ Related Files│                  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘                  │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                         │
│                                      ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                           核心处理层                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │   │
│  │  │                    文件类型检测器                                  │  │   │
│  │  │  History.jsonl  │  Session.jsonl  │  Subagent.jsonl              │  │   │
│  │  └───────────────────────────────────────────────────────────────────┘  │   │
│  │                                    │                                     │   │
│  │                                    ▼                                     │   │
│  │  ┌───────────────────┐  ┌───────────────────┐  ┌──────────────────┐    │   │
│  │  │ HistoryParser     │  │ SessionLogParser  │  │ SubagentLogParser│    │   │
│  │  │ - 时间线分析      │  │ - 对话链构建      │  │ - 事件链分析     │    │   │
│  │  │ - 命令统计        │  │ - 工具调用追踪    │  │ - 工具分组       │    │   │
│  │  │ - 活动模式        │  │ - 文件操作记录    │  │ - 用户查询提取   │    │   │
│  │  └───────────────────┘  └───────────────────┘  └──────────────────┘    │   │
│  │                                    │                                     │   │
│  │                                    ▼                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │   │
│  │  │                     HTML 生成器                                   │  │   │
│  │  │  generate_history_html() │ generate_session_html() │              │  │   │
│  │  │  generate_subagent_html()                                         │  │   │
│  │  └───────────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                         │
│                                      ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                          数据存储层                                      │   │
│  │  ~/.claude/history.jsonl     ~/.claude/projects/<sessionId>.jsonl       │   │
│  │  ~/.claude/projects/<sessionId>/subagents/agent-*.jsonl                 │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 支持的日志类型

| 类型 | 文件路径 | 说明 |
|------|----------|------|
| **History** | `~/.claude/history.jsonl` | 历史记录日志，包含所有会话的命令历史 |
| **Session** | `~/.claude/projects/<sessionId>.jsonl` | 单个会话的完整日志，包含对话、工具调用等 |
| **Subagent** | `~/.claude/projects/<sessionId>/subagents/agent-*.jsonl` | 子代理日志，记录 Agent 执行过程 |

## 项目结构

```
log_分析/
├── visualizer_server.py      # Flask Web 服务器（主程序）
├── history_visualizer.py      # History 日志 CLI 工具
├── subagent_log_visualizer.py # Subagent 日志 CLI 工具
├── 启动可视化工具.bat         # Windows 快捷启动脚本
├── 审查visualizer_server.md   # 代码审查报告
├── README.md                  # 本文档
└── ARCHITECTURE.md            # 架构详细文档
```

## 快速开始

### 方式一：Web 界面（推荐）

```bash
# 进入项目目录
cd ~/workspace/github/claude_guide/log_分析

# 启动 Web 服务器
python visualizer_server.py

# 浏览器访问
# http://localhost:5000
```

### 方式二：命令行工具

```bash
# 可视化 History 日志
python history_visualizer.py ~/.claude/history.jsonl

# 可视化 Subagent 日志
python subagent_log_visualizer.py ~/.claude/projects/<session>/subagents/agent-xxx.jsonl

# 自动打开生成的 HTML
python history_visualizer.py ~/.claude/history.jsonl --open
```

## 使用方法

### Web 界面使用

1. **启动服务器**
   ```bash
   python visualizer_server.py
   ```

2. **打开浏览器访问** `http://localhost:5000`

3. **选择日志文件**
   - 点击上传区域选择文件
   - 或拖拽 .jsonl 文件到上传区域
   - 或点击"快速打开常用文件"中的快捷按钮

4. **查看可视化结果**
   - 统计信息卡片
   - 时间线视图
   - 工具调用分析
   - 相关文件链接

5. **导出报告**
   - 点击"下载 HTML"按钮保存可视化报告

### 命令行工具使用

#### History Visualizer

```bash
# 基本用法
python history_visualizer.py <input.jsonl>

# 指定输出文件
python history_visualizer.py input.jsonl -o output.html

# 生成后自动打开浏览器
python history_visualizer.py input.jsonl --open
```

#### Subagent Log Visualizer

```bash
# 基本用法
python subagent_log_visualizer.py <agent-xxx.jsonl>

# 指定输出文件
python subagent_log_visualizer.py input.jsonl -o output.html

# 自动打开
python subagent_log_visualizer.py input.jsonl --open
```

## 日志文件结构

### History 日志格式

```json
{"timestamp": 1234567890000, "sessionId": "xxx", "project": "/path/to/project", "display": "user message"}
{"timestamp": 1234567891000, "sessionId": "xxx", "project": "/path/to/project", "display": "/model"}
```

### Session 日志格式

```json
{"type": "user", "uuid": "xxx", "timestamp": "2024-01-17T10:00:00Z", "message": {"content": [...]}}
{"type": "assistant", "uuid": "xxx", "timestamp": "2024-01-17T10:00:01Z", "message": {"content": [...], "model": "claude-3"}}
{"type": "file-history-snapshot", "timestamp": "2024-01-17T10:00:02Z", "snapshot": {...}}
```

### Subagent 日志格式

```json
{"type": "user", "agentId": "xxx", "sessionId": "xxx", "timestamp": "2024-01-17T10:00:00Z", "message": {...}}
{"type": "assistant", "agentId": "xxx", "sessionId": "xxx", "timestamp": "2024-01-17T10:00:01Z", "message": {...}}
{"type": "progress", "agentId": "xxx", "sessionId": "xxx", "timestamp": "2024-01-17T10:00:02Z", "message": {...}}
```

## 可视化内容

### History 日志可视化

- 📊 **统计概览** - 总条目数、会话数、项目数、时间跨度
- 📈 **命令使用** - 最常用的斜杠命令统计
- 📅 **活动模式** - 按小时的活动分布图
- 🗂️ **项目列表** - 使用最多的项目
- 📜 **时间线** - 最近的 100 条记录

### Session 日志可视化

- 📋 **会话元数据** - 工作目录、Git 分支、版本信息
- 💬 **对话时间线** - 用户、助手、系统消息的完整对话链
- 🔧 **工具调用** - 所有工具调用及其结果
- 📁 **文件操作** - 文件历史快照记录
- ⚠️ **系统事件** - API 错误和重试信息

### Subagent 日志可视化

- 🎯 **用户查询** - 触发 Agent 的原始请求
- 📊 **事件统计** - AI 调用、工具调用、用户消息数量
- 🔧 **工具分组** - 按工具类型分组的调用记录
- 📜 **事件时间线** - 带类型标注的事件流

## 相关文件关联

系统会自动发现并显示相关的日志文件：

| 当前文件 | 可关联的文件 |
|----------|-------------|
| History | 该时间段内的 Session 文件 |
| Session | 该会话的 Subagent 文件 |
| Subagent | 父 Session 文件 |

## 技术栈

| 组件 | 技术 |
|------|------|
| Web 框架 | Flask |
| 数据处理 | Python JSON, datetime, pathlib |
| HTML 生成 | 模板字符串 + 内联 CSS/JS |
| 文件类型检测 | JSON Schema 推断 |
| 浏览器兼容 | 现代浏览器（ES6+） |

## 常见问题

### Q: 端口 5000 被占用怎么办？

修改 `visualizer_server.py` 最后一行：
```python
app.run(host='127.0.0.1', port=8080, debug=False)
```

### Q: 日志文件很大怎么办？

Web 界面会限制显示最近 100 条记录。命令行工具也会做同样的限制。

### Q: 生成的 HTML 如何分享？

生成的 HTML 文件是独立的，可以直接通过邮件或文件共享方式分发。

### Q: 如何添加自定义样式？

编辑 `visualizer_server.py` 中的 `HTML_TEMPLATE` 变量，修改 CSS 部分。

## 开发说明

### 添加新的日志类型解析器

1. 创建新的 Parser 类
2. 实现 `parse()` 方法返回结构化数据
3. 添加对应的 HTML 生成函数
4. 在 `detect_file_type()` 中添加类型检测逻辑
5. 在 Flask 路由中添加处理分支

### 扩展统计功能

编辑对应 Parser 类的 `_build_statistics()` 方法，添加新的统计指标。

## 许可证

MIT License

## 作者

kunge2013
